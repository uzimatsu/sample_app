<li>
  <%= gravatar_for user %>
  <%= link_to user.name, user %>

  <% if current_user.admin? && !current_user?(user) %>
  | <%= link_to "ユーザーを削除", user, method: :delete, data: {confirm: "本当に削除してもいいですか??"}%>
  <% end %>
</li>
