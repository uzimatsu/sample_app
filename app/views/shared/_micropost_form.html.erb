<%= form_for(@micropost) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: "今どうしている…？" %>
  </div>
  <%= f.submit "ツ○ート", class: "btn btn-large btn-primary" %>
<% end %>
